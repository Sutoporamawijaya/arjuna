import sys
import time
import os
import random
for time import sleep

def load(s):
   for c in s + '\n':
       sys.stdout.write(c)
       sys.stdout.flush()
       time.sleep(random.random ( + 0.4))
    print "Loading...."
    time.sleep(0.5)
    load('[>>>>>>>>>>>>>>>>>]
    load()
